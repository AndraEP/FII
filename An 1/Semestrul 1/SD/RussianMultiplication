#include <iostream>

using namespace std;

int s, a, b;

int main()
{
    cin >> a >> b;
    if (a>b) swap (a, b);
    while (a)
    {
        if (a%2==1) s+=b;
        a/=2;
        b*=2;
    }
    cout << s;
}
